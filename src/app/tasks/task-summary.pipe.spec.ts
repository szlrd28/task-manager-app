import { TaskSummaryPipe } from './task-summary.pipe';

describe('TaskSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
